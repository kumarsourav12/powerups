# Powerups

Portable skills for coding agents.

The first canonical skill is [`human-docs`](skills/human-docs/SKILL.md). The catalog in `registry/skills.json` records installable skills and their content checksums.

Run the catalog check with `npm test -- test/catalog.test.mjs`.
