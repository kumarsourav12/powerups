# Adapters

Canonical skills remain in `skills/`. This directory is reserved for future
agent-specific package metadata and generated layouts. Native adapters are
added only after their public schema and installation flow are validated.

The portable `plugin.json` and the npm installer provide the initial
integration paths; this directory does not duplicate skill instructions.
